# Database Configuration for Liberty Server
# Copy this file to .env and update with your actual values

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=usersdb
DB_USER=postgres
DB_PASSWORD=postgres
